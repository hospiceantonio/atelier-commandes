/* =========================================================
   Configuration du projet Supabase.
   - supabaseUrl : Supabase -> Settings -> API -> Project URL
   - supabaseAnonKey : la clé « anon / public » du même écran
   Ces deux valeurs sont publiques par conception : la sécurité
   repose sur les règles RLS du fichier supabase/schema.sql.
   ========================================================= */
window.ATELIER_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
